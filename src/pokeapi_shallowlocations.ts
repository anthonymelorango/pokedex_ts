export type ShallowLocations = {
    count: number
    next: string
    previous: any
    results: ShallowLocation[]
};

export type ShallowLocation = {
    name: string
    url: string
};
